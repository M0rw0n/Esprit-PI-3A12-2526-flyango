security:
    password_hashers:
        App\Entity\User:
            algorithm: auto

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
<<<<<<< HEAD
            custom_authenticators:
                - App\Security\RecaptchaLoginAuthenticator
=======
            form_login:
                login_path: app_login
                check_path: app_login
                default_target_path: app_after_login
                always_use_default_target_path: true
>>>>>>> testsisi
            logout:
                path: app_logout
                target: home
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800

    access_control:
        - { path: ^/admin, roles: ROLE_ADMIN }
        - { path: ^/mon-espace, roles: ROLE_USER }
        - { path: ^/mes-, roles: ROLE_USER }
        - { path: ^/messenger, roles: ROLE_USER }
        - { path: ^/api/messages, roles: ROLE_USER }
        - { path: ^/api/groups, roles: ROLE_USER }
        - { path: ^/api/calls, roles: ROLE_USER }
        - { path: ^/chatbot, roles: PUBLIC_ACCESS }
        - { path: ^/login, roles: PUBLIC_ACCESS }
        - { path: ^/register, roles: PUBLIC_ACCESS }
        - { path: ^/api/auth, roles: PUBLIC_ACCESS }
        - { path: ^/connect, roles: PUBLIC_ACCESS }
        - { path: ^/reset-password, roles: PUBLIC_ACCESS }

    role_hierarchy:
        ROLE_ADMIN: ROLE_USER
