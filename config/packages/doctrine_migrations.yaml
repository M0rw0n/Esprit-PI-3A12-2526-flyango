doctrine_migrations:
    migrations_paths:
        # namespace is arbitrary but should be different from your application's namespace
        'DoctrineMigrations': '%kernel.project_dir%/src/Migrations'