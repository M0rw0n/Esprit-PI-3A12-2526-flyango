{% extends 'base/base.html.twig' %}
{% block title %}Activités & Excursions — Fly&Go{% endblock %}

<<<<<<< HEAD
{% block stylesheets %}
<style>
.circuit-card:hover { transform: translateY(-4px); box-shadow: 0 8px 24px rgba(0,0,0,.15) !important; }
.hcard__wish.on i { color: var(--red) !important; }
.activity-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem; }
@media (max-width: 1200px) { .activity-grid { grid-template-columns: repeat(3, 1fr); } }
@media (max-width: 900px) { .activity-grid { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 600px) { .activity-grid { grid-template-columns: 1fr; } }
.activity-card:hover { transform: translateY(-4px); box-shadow: 0 8px 24px rgba(0,0,0,.15) !important; }
</style>
{% endblock %}

{% block body %}
=======
{% block body %}

{# Hero Section #}
>>>>>>> testsisi
<section class="circuit-hero" style="background:linear-gradient(135deg,rgba(27,58,107,0.9),rgba(0,180,216,0.8)),url('https://images.unsplash.com/photo-1527856263669-12c3a0af2aa6?w=1920&q=80');background-size:cover;background-position:center;padding:3rem 0">
    <div class="container">
        <div style="display:flex;flex-direction:column;align-items:center;text-align:center;color:#fff">
            <h1 style="font-size:2.5rem;font-weight:800;margin-bottom:.5rem">Activités & Excursions</h1>
            <p style="font-size:1.1rem;opacity:.9;margin-bottom:1.5rem">{{ activities.totalItemCount|default(activities|length) }} activités disponibles — Des expériences uniques</p>
            
            {# Main Search Bar with IA Integration #}
            <div style="background:linear-gradient(135deg,rgba(255,255,255,0.98),rgba(254,243,199,0.95));padding:.6rem;border-radius:60px;display:flex;align-items:center;gap:.5rem;width:100%;max-width:950px;box-shadow:0 8px 30px rgba(0,0,0,.2)">
                {# Search Input #}
                <div style="flex:1;display:flex;align-items:center;padding:0 1rem;border-right:2px solid #ddd">
                    <i class="fa fa-search" style="color:var(--gold);margin-right:.75rem;font-size:1.1rem"></i>
                    <input type="text" id="searchInput" placeholder="Nom, lieu, mot cle..." value="{{ q|default('') }}" 
                           style="border:none;outline:none;width:100%;font-size:1rem;padding:.6rem 0;background:transparent"
                           onkeyup="if(event.key==='Enter')syncAndSubmit()">
                </div>
                
                {# Filters #}
                <select id="lieuSelect" style="border:none;outline:none;padding:.5rem;font-size:.9rem;color:#555;cursor:pointer;min-width:110px;background:transparent"
                        onchange="syncAndSubmit()">
                    <option value="">Lieu</option>
                    <option value="Tunis" {{ lieu=='Tunis'?'selected':'' }}>Tunis</option>
                    <option value="Sousse" {{ lieu=='Sousse'?'selected':'' }}>Sousse</option>
                    <option value="Djerba" {{ lieu=='Djerba'?'selected':'' }}>Djerba</option>
                    <option value="Hammamet" {{ lieu=='Hammamet'?'selected':'' }}>Hammamet</option>
                    <option value="Tozeur" {{ lieu=='Tozeur'?'selected':'' }}>Tozeur</option>
                    <option value="Kairouan" {{ lieu=='Kairouan'?'selected':'' }}>Kairouan</option>
                    <option value="Sfax" {{ lieu=='Sfax'?'selected':'' }}>Sfax</option>
                </select>
                
                <select id="catSelect" style="border:none;outline:none;padding:.5rem;font-size:.9rem;color:#555;cursor:pointer;min-width:100px;background:transparent"
                        onchange="syncAndSubmit()">
                    <option value="">Type</option>
                    <option value="Aventure" {{ categorie=='Aventure'?'selected':'' }}>Aventure</option>
                    <option value="Culture" {{ categorie=='Culture'?'selected':'' }}>Culture</option>
                    <option value="Nature" {{ categorie=='Nature'?'selected':'' }}>Nature</option>
                    <option value="Sport" {{ categorie=='Sport'?'selected':'' }}>Sport</option>
                    <option value="Gastronomie" {{ categorie=='Gastronomie'?'selected':'' }}>Gastronomie</option>
                </select>
                
                {# IA Voice Button #}
                <button type="button" id="aiVoiceBtn" onclick="startActivityVoiceSearch()" 
                        style="background:linear-gradient(135deg,var(--gold),#f59e0b);border:none;padding:.5rem;border-radius:50%;cursor:pointer;color:#fff" title="Recherche vocale IA">
                    <i class="fa fa-microphone"></i>
                </button>
                
                {# Submit Button #}
                <button type="button" onclick="syncAndSubmit()" style="background:linear-gradient(135deg,var(--navy),var(--cyan));color:#fff;border:none;padding:.75rem 1.25rem;border-radius:40px;font-weight:700;cursor:pointer">
                    <i class="fa fa-search"></i>
                </button>
            </div>
            
            {# AI Helper Text #}
            <div style="display:flex;align-items:center;gap:1rem;margin-top:1rem;opacity:.9;font-size:.9rem">
                <span><i class="fa fa-lightbulb-o" style="color:var(--gold)"></i> Tip: Dictez l'activite Rechercher avec le micro <i class="fa fa-microphone"></i></span>
            </div>
            
            {# Quick Stats #}
            <div style="display:flex;gap:2rem;margin-top:1.5rem;opacity:.85">
                <span><i class="fa fa-hiking"></i> Activités</span>
                <span><i class="fa fa-map-marker-alt"></i> Destinations</span>
                <span><i class="fa fa-calendar-check"></i> Disponibles</span>
            </div>
        </div>
    </div>
</section>

{# Filter Bar #}
<div style="background:#fff;border-bottom:1px solid var(--border);padding:.75rem 0">
    <div class="container">
        <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:.75rem">
            {# Sort & Price #}
            <div style="display:flex;align-items:center;gap:.75rem">
                <select id="triSelect" style="border:1px solid var(--border);padding:.4rem .75rem;border-radius:20px;font-size:.85rem;cursor:pointer;background:transparent"
                        onchange="syncAndSubmit()">
                    <option value="recent" {{ tri|default('recent')=='recent'?'selected':'' }}>Plus récents</option>
                    <option value="price_asc" {{ tri=='price_asc'?'selected':'' }}>Prix ↑</option>
                    <option value="price_desc" {{ tri=='price_desc'?'selected':'' }}>Prix ↓</option>
                </select>
                <input type="number" id="prixMaxInput" value="{{ prixMax|default('') }}" placeholder="Prix max" 
                       style="border:1px solid var(--border);padding:.4rem .75rem;border-radius:20px;font-size:.85rem;width:100px;background:transparent"
                       onchange="syncAndSubmit()">
                <a href="{{ path('activity_index') }}" style="color:var(--muted);text-decoration:none;font-size:.85rem">
                    <i class="fa fa-times"></i> Reset
                </a>
            </div>
        </div>
    </div>
</div>

{# Hidden Filter Form #}
<form method="GET" action="{{ path('activity_index') }}" id="filterForm" style="position:absolute;left:-9999px;width:1px">
    <input type="text" name="q" id="filterQ" value="{{ q|default('') }}">
    <input type="text" name="lieu" id="filterLieu" value="{{ lieu|default('') }}">
    <input type="text" name="categorie" id="filterCat" value="{{ categorie|default('') }}">
    <input type="text" name="tri" id="filterTri" value="{{ tri|default('recent') }}">
    <input type="text" name="prix_max" id="filterPrix" value="{{ prixMax|default('') }}">
</form>

<script>
function syncAndSubmit() {
    document.getElementById('filterQ').value = document.getElementById('searchInput').value;
    document.getElementById('filterLieu').value = document.getElementById('lieuSelect').value;
    document.getElementById('filterCat').value = document.getElementById('catSelect').value;
    document.getElementById('filterTri').value = document.getElementById('triSelect').value;
    document.getElementById('filterPrix').value = document.getElementById('prixMaxInput').value;
    document.getElementById('filterForm').submit();
}
</script>

{# Main Content #}
<div class="container" style="padding:2rem 0">
    
    {# Results Header #}
    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:1.5rem">
        <div>
            <h2 style="font-size:1.25rem;font-weight:700;color:var(--text)">{{ activities.totalItemCount|default(activities|length) }} activité{{ activities.totalItemCount|default(activities|length)>1?'s':'' }}</h2>
            <p style="font-size:.85rem;color:var(--muted)">Réservation instantanée · Activités certifiées</p>
        </div>
<<<<<<< HEAD
        <div style="display:flex;align-items:center;gap:.75rem">
            <a href="{{ path('activity_discovery') }}" style="background:linear-gradient(135deg,var(--gold),#f59e0b);color:#fff;padding:.5rem 1rem;border-radius:25px;text-decoration:none;font-size:.85rem;font-weight:700;box-shadow:0 4px 12px rgba(245,158,11,0.2)">
                <i class="fa fa-search"></i> Découverte Réelle
            </a>
            <a href="{{ path('activity_nearby_map') }}" style="background:linear-gradient(135deg,var(--navy),var(--cyan));color:#fff;padding:.5rem 1rem;border-radius:25px;text-decoration:none;font-size:.85rem;font-weight:600">
                <i class="fa fa-map-marker-alt"></i> Autour de moi
            </a>
=======
        <div style="display:flex;gap:.5rem">
>>>>>>> testsisi
            <span style="display:inline-flex;align-items:center;gap:.4rem;padding:.4rem .85rem;border-radius:100px;background:var(--bg);border:1px solid var(--border);font-size:.75rem;font-weight:600;color:var(--muted)">
                <i class="fa fa-shield-alt" style="color:var(--cyan)"></i> Réservation sécurisée
            </span>
        </div>
    </div>

<<<<<<< HEAD
    {% if activities|length > 0 %}
    <div id="activity-grid" class="activity-grid">
        {% for a in activities %}
        <article class="activity-card" data-id="{{ a.id }}" onclick="window.location='{{ path('activity_show', {id: a.id}) }}'" style="background:#fff;border-radius:24px;overflow:hidden;box-shadow:0 10px 30px rgba(0,0,0,0.05);transition:all .3s cubic-bezier(0.4, 0, 0.2, 1);cursor:pointer;border:1px solid var(--border);position:relative">
            <div style="position:relative;height:200px;overflow:hidden">
                {% if a.image %}
                <img src="{{ asset(a.image) }}" style="width:100%;height:100%;object-fit:cover;transition:transform 0.5s" alt="{{ a.title }}" class="activity-img" onerror="this.src='https://images.unsplash.com/photo-1527856263669-12c3a0af2aa6?w=600&q=80'">
                {% else %}
                <img src="https://images.unsplash.com/photo-1527856263669-12c3a0af2aa6?w=600&q=80" style="width:100%;height:100%;object-fit:cover" alt="{{ a.title }}">
                {% endif %}
                
                <div style="position:absolute;top:1rem;left:1rem;display:flex;flex-direction:column;gap:0.5rem">
                    {% if a.category %}
                    <span style="background:rgba(255,255,255,0.9);backdrop-filter:blur(4px);color:var(--navy);padding:.35rem .75rem;border-radius:12px;font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:0.02em">
                        {{ a.category }}
                    </span>
                    {% endif %}
                </div>

                {% if loop.index <= 2 %}
                <span style="position:absolute;top:1rem;right:1rem;background:linear-gradient(135deg,var(--gold),#f59e0b);color:#fff;padding:.35rem .75rem;border-radius:12px;font-size:.7rem;font-weight:800;box-shadow:0 4px 10px rgba(0,0,0,0.2)">
                    <i class="fa fa-fire"></i> POPULAIRE
                </span>
                {% endif %}
                
                <button class="hcard__wish" onclick="event.stopPropagation(); toggleFavorite({{ a.id }}, 'activity', this)" style="position:absolute;bottom:1rem;right:1rem;background:#fff;width:40px;height:40px;border-radius:12px;display:flex;align-items:center;justify-content:center;border:none;cursor:pointer;box-shadow:0 4px 15px rgba(0,0,0,0.1);transition:all 0.2s">
                    <i class="fa fa-heart" style="color:var(--red)"></i>
                </button>
            </div>
            <div style="padding:1.5rem">
                <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.75rem">
                    <div style="display:flex;align-items:center;gap:.5rem;font-size:.8rem;color:var(--muted);font-weight:600">
                        <i class="fa fa-map-marker-alt" style="color:var(--cyan)"></i>
                        {{ a.lieu ?: 'Tunisie' }}
                    </div>
                    {% if a.reviews|length > 0 %}
                    <div style="display:flex;align-items:center;gap:0.25rem;font-size:0.85rem;font-weight:700;color:var(--navy)">
                        <i class="fa fa-star" style="color:var(--gold)"></i> {{ a.noteMoyenne|default(0)|number_format(1) }}
                    </div>
                    {% endif %}
                </div>
                
                <h3 style="font-size:1.15rem;font-weight:800;margin-bottom:.75rem;color:var(--navy);display:-webkit-box;-webkit-line-clamp:1;-webkit-box-orient:vertical;overflow:hidden">{{ a.title }}</h3>
                
                <div style="display:flex;gap:1rem;font-size:.8rem;color:var(--muted);margin-bottom:1.5rem;background:var(--bg);padding:0.5rem;border-radius:10px">
                    {% if a.duration %}<span style="display:flex;align-items:center;gap:0.35rem"><i class="fa fa-clock" style="color:var(--cyan)"></i> {{ a.duration }}</span>{% endif %}
                    <span style="display:flex;align-items:center;gap:0.35rem"><i class="fa fa-users" style="color:var(--cyan)"></i> {{ a.capacity }} places</span>
                </div>
                
                <div style="display:flex;justify-content:space-between;align-items:center;padding-top:1rem;border-top:1px dashed var(--border)">
                    <div>
                        <span style="font-size:1.6rem;font-weight:900;color:var(--navy);letter-spacing:-0.02em">{{ a.price|number_format(0,'','') }}</span>
                        <span style="color:var(--muted);font-size:.85rem;font-weight:600">TND</span>
                    </div>
                    <div style="display:flex;gap:.5rem" onclick="event.stopPropagation()">
                        <a href="{{ path('activity_show', {id: a.id}) }}" class="btn btn-navy" style="padding:.6rem 1.2rem;border-radius:14px;font-size:.85rem;font-weight:700;text-decoration:none;display:flex;align-items:center;gap:0.5rem">
                            Réserver <i class="fa fa-chevron-right" style="font-size:0.7rem"></i>
                        </a>
=======
    {% if activities %}
    <div id="activity-grid" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:1.5rem">
        {% for a in activities %}
        <article class="activity-card" data-id="{{ a.id }}" onclick="window.location='{{ path('activity_show', {id: a.id}) }}'" style="background:#fff;border-radius:var(--r);overflow:hidden;box-shadow:0 2px 8px rgba(0,0,0,.08);transition:all .3s;cursor:pointer">
            <div style="position:relative;height:180px;overflow:hidden">
                {% if a.image %}
                <img src="{{ asset(a.image) }}" style="width:100%;height:100%;object-fit:cover" alt="{{ a.title }}" onerror="this.src='https://images.unsplash.com/photo-1527856263669-12c3a0af2aa6?w=600&q=80'">
                {% else %}
                <img src="https://images.unsplash.com/photo-1527856263669-12c3a0af2aa6?w=600&q=80" style="width:100%;height:100%;object-fit:cover" alt="{{ a.title }}">
                {% endif %}
                {% if loop.index <= 2 %}
                <span style="position:absolute;top:.5rem;right:.5rem;background:var(--gold);color:var(--navy-dd);padding:.25rem .5rem;border-radius:15px;font-size:.7rem;font-weight:800">
                    <i class="fa fa-fire"></i> Populaire
                </span>
                {% endif %}
                <button class="hcard__wish" onclick="event.stopPropagation(); toggleFavorite({{ a.id }}, 'activity', this)" style="position:absolute;bottom:.5rem;right:.5rem;background:#fff;width:36px;height:36px;border-radius:50%;display:flex;align-items:center;justify-content:center;border:none;cursor:pointer;box-shadow:0 2px 8px rgba(0,0,0,.15)">
                    <i class="fa fa-heart" style="color:var(--red)"></i>
                </button>
                {% if a.reviews|length > 0 %}
                <div style="position:absolute;bottom:.5rem;left:.5rem;background:rgba(0,0,0,.7);color:#fff;padding:.25rem .5rem;border-radius:15px;font-size:.7rem">
                    <i class="fa fa-star" style="color:var(--gold)"></i> {{ a.noteMoyenne|default(0)|number_format(1) }} ({{ a.reviews|length }})
                </div>
                {% endif %}
            </div>
            <div style="padding:1rem">
                <div style="display:flex;align-items:center;gap:.5rem;font-size:.8rem;color:var(--muted);margin-bottom:.5rem">
                    <i class="fa fa-map-marker-alt" style="color:var(--cyan)"></i>
                    {{ a.lieu ?: 'Tunisie' }}
                </div>
                <h3 style="font-size:1.1rem;font-weight:700;margin-bottom:.5rem;color:var(--text);white-space:nowrap;overflow:hidden;text-overflow:ellipsis">{{ a.title }}</h3>
                <div style="display:flex;gap:1rem;font-size:.8rem;color:var(--muted);margin-bottom:1rem">
                    {% if a.duration %}<span><i class="fa fa-clock"></i> {{ a.duration }}</span>{% endif %}
                    <span><i class="fa fa-users"></i> {{ a.capacity }} places</span>
                </div>
                <div style="display:flex;justify-content:space-between;align-items:center">
                    <div>
                        <span style="font-size:1.5rem;font-weight:800;color:var(--navy)">{{ a.price|number_format(0,'','') }}</span>
                        <span style="color:var(--muted);font-size:.85rem">TND</span>
                    </div>
                    <div style="display:flex;gap:.5rem" onclick="event.stopPropagation()">
                        <a href="{{ path('activity_show', {id: a.id}) }}" style="padding:.4rem .75rem;border:1px solid var(--navy);border-radius:20px;font-size:.8rem;color:var(--navy);text-decoration:none">Détails</a>
                        <a href="{{ path('activity_show', {id: a.id}) }}" style="padding:.4rem .75rem;background:var(--navy);border-radius:20px;font-size:.8rem;color:#fff;text-decoration:none">Réserver</a>
>>>>>>> testsisi
                    </div>
                </div>
            </div>
        </article>
        {% endfor %}
    </div>
    
    {# Pagination #}
    {% if activities.pageCount|default(1) > 1 %}
    <div style="display:flex;justify-content:center;align-items:center;gap:.5rem;margin-top:2rem;flex-wrap:wrap">
        {% if activities.currentPageNumber|default(1) > 1 %}
        <a href="{{ path('activity_index', app.request.query|merge({page: activities.currentPageNumber|default(1) - 1})) }}" style="padding:.5rem 1rem;border:1px solid var(--border);border-radius:25px;color:var(--navy);background:#fff;font-weight:600;transition:all .2s">← Précédent</a>
        {% endif %}
        {% if activities.currentPageNumber|default(1) > 3 %}
        <a href="{{ path('activity_index', app.request.query|merge({page: 1})) }}" style="padding:.5rem .75rem;border:1px solid var(--border);border-radius:50%;color:var(--text);background:#fff">1</a>
        <span style="color:var(--muted)">...</span>
        {% endif %}
        {% for page in 1..activities.pageCount|default(1) %}
        {% if page >= activities.currentPageNumber|default(1) - 1 and page <= activities.currentPageNumber|default(1) + 1 %}
        <a href="{{ path('activity_index', app.request.query|merge({page: page})) }}" style="min-width:40px;height:40px;display:flex;align-items:center;justify-content:center;border:1px solid var(--navy);border-radius:50%;background:{{ page==activities.currentPageNumber|default(1)?'var(--navy)':'#fff'}};color:{{ page==activities.currentPageNumber|default(1)?'#fff':'var(--navy)' }};font-weight:600">{{ page }}</a>
        {% endif %}
        {% endfor %}
        {% if activities.currentPageNumber|default(1) < activities.pageCount|default(1) - 2 %}
        <span style="color:var(--muted)">...</span>
        <a href="{{ path('activity_index', app.request.query|merge({page: activities.pageCount|default(1)})) }}" style="padding:.5rem .75rem;border:1px solid var(--border);border-radius:50%;color:var(--text);background:#fff">{{ activities.pageCount|default(1) }}</a>
        {% endif %}
        {% if activities.currentPageNumber|default(1) < activities.pageCount|default(1) %}
        <a href="{{ path('activity_index', app.request.query|merge({page: activities.currentPageNumber|default(1) + 1})) }}" style="padding:.5rem 1rem;border:1px solid var(--border);border-radius:25px;color:var(--navy);background:#fff;font-weight:600;transition:all .2s">Suivant →</a>
        {% endif %}
    </div>
    {% endif %}
    
    {% else %}
    <div style="text-align:center;padding:4rem 2rem">
        <i class="fa fa-hiking" style="font-size:3rem;color:var(--muted);margin-bottom:1rem;display:block"></i>
        <h3 style="font-size:1.25rem;font-weight:700;margin-bottom:.5rem">Aucune activité trouvée</h3>
        <p style="color:var(--muted);margin-bottom:1.5rem">Essayez de modifier vos critères</p>
        <a href="{{ path('activity_index') }}" style="background:var(--navy);color:#fff;padding:.75rem 1.5rem;border-radius:25px;text-decoration:none;font-weight:600">Voir toutes les activités</a>
    </div>
    {% endif %}
</div>

{# Scripts for Weather #}
<link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.10/index.global.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.10/index.global.min.js"></script>

<script>
async function searchWeather(query) {
    const result = document.getElementById('customWeatherResult');
    if (!query || query.length < 2) { result.style.display = 'none'; return; }
    result.style.display = 'inline-flex';
    result.innerHTML = '<i class="fa fa-spinner fa-spin"></i> <span style="margin-left:8px">Recherche...</span>';
    try {
        const res = await fetch('/api/weather?city=' + encodeURIComponent(query));
        const data = await res.json();
        if (data.success && data.weather) {
            const w = data.weather;
            const icons = {'01d':'☀️','01n':'🌙','02d':'⛅','03d':'☁️','09d':'🌧️','10d':'🌦️','11d':'⛈️','13d':'❄️','50d':'🌫️'};
            result.innerHTML = '<span style="margin-right:8px">' + (icons[w.icon] || '🌤️') + '</span>' +
                '<span style="font-weight:700">' + w.temperature + '°C</span>' +
                '<span style="margin-left:8px;opacity:0.9">' + w.city + '</span>';
        } else {
            result.innerHTML = '<i class="fa fa-exclamation-circle"></i> <span style="margin-left:8px">Non trouvé</span>';
            setTimeout(() => result.style.display = 'none', 2000);
        }
    } catch (e) {
        result.innerHTML = '<i class="fa fa-exclamation-triangle"></i> <span style="margin-left:8px">Erreur</span>';
    }
}

let activityAiRecognition = null;

function startActivityVoiceSearch() {
    if (!('webkitSpeechRecognition' in window) && !('SpeechRecognition' in window)) {
        alert('Reconnaissance vocale non supportee');
        return;
    }

    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
    activityAiRecognition = new SpeechRecognition();
    activityAiRecognition.lang = 'fr-FR';
    activityAiRecognition.interimResults = false;
    activityAiRecognition.maxAlternatives = 1;

    const btn = document.getElementById('aiVoiceBtn');
    const input = document.getElementById('searchInput');
    btn.innerHTML = '<i class="fa fa-spinner fa-spin"></i>';

    activityAiRecognition.onresult = function(event) {
        const transcript = event.results[0][0].transcript;
        input.value = transcript;
        setTimeout(function() {
            syncAndSubmit();
        }, 300);
    };

    activityAiRecognition.onerror = function(event) {
        console.error('Voice recognition error', event.error);
        btn.innerHTML = '<i class="fa fa-microphone"></i>';
        alert('Erreur de reconnaissance vocale: ' + event.error);
    };

    activityAiRecognition.onend = function() {
        btn.innerHTML = '<i class="fa fa-microphone"></i>';
    };

    activityAiRecognition.start();
}
</script>
<<<<<<< HEAD
=======

<style>
.circuit-card:hover { transform: translateY(-4px); box-shadow: 0 8px 24px rgba(0,0,0,.15) !important; }
.hcard__wish.on i { color: var(--red) !important; }
</style>
>>>>>>> testsisi
{% endblock %}